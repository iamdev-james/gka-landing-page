import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Genius Khalifah Academy — Nurturing Minds, Shaping Souls",
  description:
    "A WAEC & NECO approved private school in Agbado, Ogun State, Nigeria. Kindergarten through Secondary, plus Arabic & Islamic School.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} antialiased`}>{children}</body>
    </html>
  );
}
